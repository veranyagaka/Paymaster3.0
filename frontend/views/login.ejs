<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Paymaster- Login Form</title>
  <link rel="stylesheet" href="/css/registration.css">
</head>
<body>
  <div class="wrapper">
    <form action="/login" method="POST">
      <h2>Employee Login</h2>
      <h3>Welcome back</h3>

        <div class="input-field">
        <input type="text" id="employeeID" name="employeeID" required>
        <label>Enter your EmployeeID</label>
      </div>     
      <div class="input-field">
        <input type="password" id="password" name="password" required>
        <label>Enter your password</label>
      </div>
      <div class="forget">
        <label for="remember">
          <input type="checkbox" id="remember">
          <p>Remember me</p>
        </label>
      </div>
      <button type="submit">Log In</button>
      <div class="register">
          <a href="reset_password" class="forgot-link">Forgot Username / Password?</a>
          <p>Don't have an account? <a href="register">Sign Up</a></p>
          <a class="btn" href="admin-login" style="color: rgb(12, 82, 82);">Login Here For Administrators</a>

	</div>
    </form>
  </div>
  <script>
    function handleSubmit(event) {
      event.preventDefault();
      const formData = {
        employeeID: event.target.employeeID.value,
        email: event.target.email.value,
        password: event.target.password.value,
      };
      console.log('Form submitted:', formData);
      // Add your form submission logic here
    }
  </script>
</body>
</html>
  